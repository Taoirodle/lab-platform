// L.A.B Hub — front-end config. The native shell can override SERVER at launch
// (LAB_SERVER env). Falls back to the LAN server for browser dev.
window.LAB_CONFIG = {
  SERVER: 'http://192.168.1.115:8090',
  APP_VERSION: '0.2.4'
};
